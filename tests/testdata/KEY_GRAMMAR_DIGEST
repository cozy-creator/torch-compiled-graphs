# sha256 of testdata/compiled_graph_key_vectors.json — the SHARED compiled-graph
# key grammar corpus (th#1897). python-gen-worker commits this same digest beside
# its own byte-identical copy. Tensorhub's three-way external parity fence
# (th#1914, tensorhub PR #1186) compares this package, python-gen-worker, and
# the Hub. Changing the corpus is a coupled cross-repo cut: pgw lands first,
# then both vendored copies and the external fence move in one window.
a274c5702b8c167ca119758d9fb1f98c40ddfdb3dbf6c58b4aacd580803e8c2b
